[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn stunting_project.wsgi --bind 0.0.0.0:$PORT --workers 1 --timeout 120"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
